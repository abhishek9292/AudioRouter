

pip install pythonnet
pip install pyinstaller
pip install -r requirements.txt

pip freeze > requirements.txt

Before to get exe run : 
   ```sh
    pip install -e .
   ```
- **Basic build:**
    ```sh
    pyinstaller --windowed main.py 
    ```
- **Build with a custom icon:**
    ```sh
    pyinstaller --onefile --windowed --noupx --version-file=file_version.txt --icon=assets/icons/clock_icon.ico --name "Tray iClock" main.py
    ```

    pyinstaller --onefile --windowed --add-data "assets;assets" --icon=app.ico --name "MyApp" adds-route.py
         pip install pyinstaller