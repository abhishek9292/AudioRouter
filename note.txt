pip install pythonnet
pip install pyinstaller
pip install -r requirements.txt

pip freeze > requirements.txt


- **Basic build:**
    ```sh
    pyinstaller --windowed adds-route.py 
    ```
- **Build with a custom icon:**
    ```sh
    pyinstaller --onefile --windowed --noupx --version-file=file_version.txt --icon=icon.ico --name "NVIDIA sMain" adds-route.py
    ```

    pyinstaller --onefile --windowed --add-data "assets;assets" --icon=app.ico --name "MyApp" adds-route.py
         pip install pyinstaller